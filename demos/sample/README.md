# sample-app

A fixture project used to render the `@whenlabs/when` README demo. Run all six
WhenLabs tools against this directory with:

```bash
when doctor
```

## Layout

- `src/index.js` — entrypoint that loads `.env` and validates it with zod
- `.env` / `.env.schema` — environment variables and their schema
