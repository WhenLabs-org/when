version: 1

variables:
  PORT:
    type: integer
    required: true
    default: 3000
    range: [1024, 65535]
    description: "HTTP port the server listens on"

  DATABASE_URL:
    type: url
    required: true
    description: "Postgres connection string"
